// the idea is to build on Bevy's built in logging

// things we might want to do:
// - send log messages to a server over BRP
// - attach a 3rd party service in a CI/CD context
// - change the filter dynamically from within the UI
